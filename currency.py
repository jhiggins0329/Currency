class Currency():
    def currency(self, currency_code, value)
    self.currrency_code = currency_code
    self.value = value
    return

    def (arg):
        pass __add__(self, other):
        if self.value == other.value
            return Currency

    def hg
